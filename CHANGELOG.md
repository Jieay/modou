# Changelog

本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/) 格式，
版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。

## [Unreleased]

## [0.2.3] - 2026-08-20

### Added

- 已打开文件外部变更自动刷新：终端或 AI 助手修改磁盘文件后，编辑器实时重新加载最新内容（保持光标与滚动位置，不触发未保存标记），无需关闭重开；文件有未保存修改时不覆盖并提示，文件被删除时状态栏提示

## [0.2.2] - 2026-08-20

### Added

- Git 状态自动刷新：窗口重新聚焦时刷新，并每 30 秒轮询一次；在终端执行 `git commit` 等操作后文件树徽章与状态栏自动更新，不再停留在旧状态

## [0.2.1] - 2026-08-19

### Fixed

- 多窗口项目状态隔离：新窗口打开项目不再把已打开窗口的文件树/git 状态覆盖成新项目（原先是全局共享状态）
- 菜单事件真正定向到焦点窗口：后端改用 `emit_to(label, ...)` 且 payload 携带窗口 label，前端校验后处理（`WebviewWindow::emit` 在 Tauri v2 是无过滤的广播，是多窗口同时打开同一项目的根因）
- Dock 图标右键选择最近项目改为在新窗口打开，不再改动当前窗口
- 菜单/Dock 打开最近项目时，若该项目已有窗口打开则直接置前该窗口，不再重复打开

## [0.2.0] - 2026-08-18

### Added

- Git 变更可视化：文件树文件右侧显示 M/A 徽章并着色、含变更的目录着色；编辑器行号槽显示行级变更（新增绿条/修改蓝条/删除红三角），与 HEAD 比较，编辑时防抖更新、保存即刷新
- 多 git 仓库工作区支持：根目录不是仓库时自动收集一级子目录中的仓库，徽章聚合所有子仓库变更，行级 diff 按文件路径自动命中所属仓库

## [0.1.9] - 2026-08-17

### Fixed

- 文件树选中态跟随活动标签：关闭/切换标签后高亮同步到当前文件，不再停留在已关闭的文件上
- 选中同步支持穿透折叠目录：目标文件在折叠的文件夹中时自动展开祖先目录并定位（修复折叠目录子节点仍在 DOM 中导致误判为已选中）

## [0.1.8] - 2026-08-17

### Added

- 图片文件预览（png/jpg/gif/webp/bmp/ico/svg）：只读预览视图、棋盘格透明底衬，底部显示分辨率与文件大小（Tauri asset 协议流式读取）

### Fixed

- 修复图片预览 403：scope 显式关闭 requireLiteralLeadingDot（Unix 默认通配符不匹配点目录），并在打开项目时动态授权 asset 协议访问项目目录
- 修复 Markdown 代码块 ```bash 无语法高亮（shell 语言注册 id 不含 bash，补注册 bash/sh 复用其词法）
- 标签悬停显示完整文件路径，长文件名截断时可查看

## [0.1.7] - 2026-08-17

### Added

- 文件树展开状态跨刷新/重启保持（含逐层懒加载恢复）
- 双击编辑器标签在文件树中展开祖先目录并定位选中文件

### Fixed

- 文件树点击文件时同步选中态，不再停留在上次定位的节点

## [0.1.6] - 2026-08-17

### Added

- 顶部项目切换器：显示当前文件夹名，下拉切换最近项目，行内单条删除记录
- 窗口标题跟随当前项目文件夹名，多窗口可在 Dock/切换器中区分
- YAML 语法高亮（.yaml/.yml）

### Fixed

- 修复窗口标题「墨斗」文字遮挡标签栏文件名（hiddenTitle）
- 新建窗口打开为空白，不再恢复主窗口会话，也不覆盖会话记录
- 文件夹展开三角改为 CSS 绘制（修复字体缺字形显示为点的问题）
- 文件树间距紧凑化；删除按钮/切换器按钮可辨识度优化

## [0.1.5] - 2026-08-17

### Added

- 全中文系统菜单（墨斗/文件/编辑/视图/窗口）
- Dock 图标右键菜单：新建窗口 + 最近打开项目
- 最近打开项目记录（系统菜单 + Dock 菜单，持久化保存，上限 10 条）
- 新建窗口（⌘⇧N）多窗口支持
- 应用显示名改为「墨斗」（菜单栏/Dock/Finder）

### Changed

- 文件树美化：SVG 图标按语言着色、树形归属虚线、间距修正

## [0.1.4] - 2026-08-17

### Added

- 文件树右键完整操作：新建文件/文件夹、复制、剪切、粘贴、复制路径、重命名、删除
- 文件树空白处右键：新建文件/文件夹/刷新
- 侧边栏与编辑区之间拖拽调宽
- dotfile 语言识别：.env/.gitignore/.dockerignore/Makefile/.toml/uv.lock 等
- 终端显示/隐藏快捷键（⌘`）与新建终端（⌘T）拆分

### Fixed

- 修正终端区拉伸方向（终端在右侧，与侧边栏相反）

## [0.1.3] - 2026-08-16

### Added

- 标签右键菜单：关闭/关闭其他标签/关闭所有标签
- 最大标签页数量设置（设置面板 + 命令面板，实时生效并持久化）
- 统一设置面板（⌘, 或左下角设置按钮）
- 语言识别扩展到 40+ 种

### Fixed

- 修复标签文件名后的长空格（标签宽度随内容自适应）

## [0.1.2] - 2026-08-16

### Fixed

- 修复窗口无法拖动、双击顶部无法最大化（改用 Tauri 原生拖拽区域）

### Added

- `make publish` 一键发布命令（构建、打 Tag、推送、创建 Release 并上传 dmg）

## [0.1.1] - 2026-08-16

### Added

- 初始版本发布
- 代码编辑：多标签页、语法高亮、行号显示
- 项目浏览：文件树、模糊搜索（⌘+P）
- 原生终端：多终端会话、PTY 支持
- Git 集成：状态栏显示分支
- 命令面板：⌘+Shift+P 快速执行命令
- VS Code Dark+ 深色主题
